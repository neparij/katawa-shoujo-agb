## KSRE converter

---

### Instructions to run

- Setup venv
  ```shell
  python3 -m venv venv
  source venv/bin/activate
  ```
- Install the `ksre_converter` package
  ```shell
  pip3 install -e .
  ```
- Use it :)
  ```
  ksre_converter --help
  ```
